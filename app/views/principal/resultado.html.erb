<h1 class="text-center my-4">Resultado da Avaliação</h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8 text-center">
      <p><strong>Olho Esquerdo:</strong> <%= @olho_esquerdo.presence || (@pl_esquerdo ? "PL" : @npl_esquerdo ? "NPL" : @indeterminado_esquerdo ? "Indeterminado" : "") %> - <%= @categoria_esquerdo %> (CID: <%= @cid_esquerdo %>)</p>

      <p><strong>Olho Direito:</strong> <%= @olho_direito.presence || (@pl_direito ? "PL" : @npl_direito ? "NPL" : @indeterminado_direito ? "Indeterminado" : "") %> - <%= @categoria_direito %> (CID: <%= @cid_direito %>)</p>

      <hr>

      <p><strong>Diagnóstico Final:</strong> <%= @descricao_cid %></p>

      <%= link_to "Voltar", root_path, class: "btn btn-primary mt-3" %>
    </div>
  </div>
</div>
